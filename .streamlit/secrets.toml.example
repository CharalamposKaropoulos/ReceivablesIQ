# Copy this file to secrets.toml and fill in values as needed.
# secrets.toml is gitignored — never commit real credentials.
#
# Example placeholders only (this demo uses local DuckDB; secrets are optional).

# [connections.duckdb]
# database = "data/database/credit_risk.duckdb"

# [general]
# demo_mode = true
